"""FuzzEngine — schema-driven API fuzzing.

Generates corpus from OpenAPI/JSON Schema, enforces iteration budget,
records every request, and captures crash-triggering requests.
"""

__version__ = "1.0.0"
