"""BicepHelper — validation and deployment helper for Bicep modules.

Wraps `az bicep build` / `bicep build` for validation and
`az deployment group create` for deployment with what-if support.
"""

__version__ = "1.0.0"
